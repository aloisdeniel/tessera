name: tessera_chess
description: A full game of chess rendered with Tessera — ported from the C example.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  ffi: ^2.1.0
  tessera:
    path: ../

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
