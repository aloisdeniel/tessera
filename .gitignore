/build/
/build-*/
/dist/
/dist-fullrun.log

# Prebuilt native libs installed by publish.sh (regenerated, never committed)
/bindings/flutter_tessera/native/
/bindings/dart/native/
*.o
*.a
*.dylib
*.so
*.spv
*.dxil
compile_commands.json
.DS_Store
/plan/scratch/

# Vendored SDL source clone (build dependency; not part of the repo)
/third_party/SDL/
# Keep the checked-in SPIR-V shader artifacts despite the *.spv rule above
!assets/shaders/*.spv
!bindings/flutter_tessera/android/src/main/assets/shaders/*.spv
